1.新建工程时候会先弹Imgui的输入窗口，然后弹系统的，有点重复
2.ExecuteBlueprint节点去掉同步逻辑，完全采用异步。输入文件可以不用扩展名，增加缺少扩展名逻辑。
3.Add Function后，直接在视图中创建Entry和Return Node, 不需要手动点击添加，函数右边添加删除按钮，并提供右键菜单。支持自定义设置Category
4.蓝图Library函数库可以直接被其他蓝图调用，右键菜单可以直接搜索到该节点，类似UE4。现在的调用方式不友好，还需要手动输入库文件path和函数functionID。
4.左侧标签页采用vs的结构，左侧一列标签页按钮，如Projects, Nodes
5.增加单步调试功能，视图中间上部显示调试工具条，底下Execute按钮放到调试工具条里面